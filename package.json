{
  "name": "huey",
  "private": true,
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "preinstall": "node scripts/check-node-version.js",
    "build:html": "node scripts/build-html.js",
    "predev": "npm run build:html",
    "dev":       "vite",
    "prebuild": "npm run build:html",
    "build":     "vite build",
    "prepreview": "npm run build:html",
    "preview":   "vite preview",
    "pretest:unit": "npm run build:html && node scripts/check-node-version.js",
    "test:unit": "vitest run",
    "pretest:coverage": "npm run build:html && node scripts/check-node-version.js",
    "test:coverage": "vitest run --coverage",
    "pretest:ui": "npm run build:html && node scripts/check-node-version.js",
    "test:ui":   "playwright test",
    "lint:js":   "eslint src/"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.49.0",
    "@vitest/coverage-v8": "^3.0.0",
    "eslint": "^10.0.2",
    "jsdom": "^24.1.3",
    "serve": "^14.2.5",
    "vite": "^7.3.1",
    "vitest": "^3.0.0"
  }
}
